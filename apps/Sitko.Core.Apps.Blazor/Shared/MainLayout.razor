@using Layout = AntDesign.ProLayout.Layout
@using Sitko.Core.App
@inherits LayoutComponentBase
@inject Application _application

<BasicLayout Title="@_application.Name" MenuData="_menuData" HeaderRender="false" Layout="Layout.Side" Logo="@("/logo.png")">
    <ChildContent>
        @Body
    </ChildContent>
    <FooterRender>
        <GlobalFooter>
            <Copyright>
                <div>
                    Sitko.ru <Icon Type="copyright"/> @DateTimeOffset.UtcNow.Year
                </div>
            </Copyright>
        </GlobalFooter>
    </FooterRender>
</BasicLayout>

@code
{
    private readonly MenuDataItem[] _menuData =
    {
        new()
        {
            Path = "/",
            Name = "Demo",
            Key = "Demo",
            Icon = "dashboard"
        },
        new()
        {
            Path = "/Options",
            Name = "Options",
            Key = "options",
            Icon = "setting"
        }
    };
}
